<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.uc.dei.cm.myfinances.myfinances">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_financeslogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_financeslogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="pt.uc.dei.cm.myfinances.activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="pt.uc.dei.cm.myfinances.activities.SignedInActivity"></activity>
    </application>

</manifest>